name: Coverage

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          # Añadimos PyYAML para que coveralls lea bien la config
          pip install coverage pytest coveralls PyYAML 

      - name: Run tests with coverage
        run: |
          # Generamos el .coverage
          coverage run -m pytest
          coverage report
          # Generamos el XML por si Coveralls lo prefiere
          coverage xml

      - name: Upload to Coveralls
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: coveralls